# Story 2.3: Aprovação e Descarte por Swipe

## Status
Draft

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["UX review", "drag interaction validation", "API integration review"]

## Story
**As a** Anderson (gestor da 4G),
**I want** arrastar o card de uma empresa para a direita para aprovar ou para a esquerda para descartar,
**so that** eu possa decidir quem vai receber o WhatsApp antes de qualquer mensagem ser enviada.

## Acceptance Criteria
1. Card pode ser arrastado para a direita (aprovar → `PROSPECTAR`) ou para a esquerda (descartar → `DESCARTADOS`)
2. Indicação visual clara durante o arraste: verde ao arrastar para direita, vermelho para esquerda
3. Empresa aprovada: status muda para `PROSPECTAR` no banco; n8n dispara WhatsApp e muda para `PROSPECTADOS`
4. Empresa descartada: status muda para `DESCARTADOS` no banco
5. Card removido do painel imediatamente após a decisão

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled
>
> Validação de qualidade usará revisão manual com foco na interação de swipe e gatilho correto do n8n.

## Tasks / Subtasks

- [ ] Task 1 — Criar endpoint PATCH /api/leads/[telefone] (AC: 3, 4)
  - [ ] Criar `src/app/api/leads/[telefone]/route.ts`
  - [ ] Aceitar autenticação por sessão Supabase (Anderson) OU por API key (n8n)
  - [ ] Validar que `status` está no dicionário oficial: `LOCALIZADOS`, `PROSPECTAR`, `PROSPECTADOS`, `INTERESSE`, `TRANSFERIDOS`, `DESCARTADOS`
  - [ ] Atualizar status do lead pelo `telefone` (chave de idempotência)
  - [ ] Retornar lead atualizado

- [ ] Task 2 — Implementar componente SwipeCard com @dnd-kit (AC: 1, 2, 5)
  - [ ] Criar `src/components/approval/SwipeCard.tsx`
  - [ ] Usar `@dnd-kit/core` para capturar o gesto de arraste
  - [ ] Calcular direção do arraste: delta X > threshold → aprovado; delta X < -threshold → descartado
  - [ ] Aplicar cor de fundo dinâmica: verde (`bg-green-100`) ao arrastar direita, vermelho (`bg-red-100`) ao arrastar esquerda
  - [ ] Animar saída do card após decisão (transição suave)
  - [ ] Chamar callback `onApprove()` ou `onDiscard()` conforme decisão

- [ ] Task 3 — Integrar SwipeCard com a API (AC: 3, 4, 5)
  - [ ] **Atualizar** `src/lib/api/leads.ts` (criado na Story 2.2) → adicionar função `updateLeadStatus(telefone, status)` — não recriar o arquivo, apenas adicionar a função
  - [ ] No callback `onApprove()`: chamar PATCH com `status: 'PROSPECTAR'` e remover card da lista local
  - [ ] No callback `onDiscard()`: chamar PATCH com `status: 'DESCARTADOS'` e remover card da lista local
  - [ ] Tratar erro: exibir toast de erro se a API falhar, manter card no painel

- [ ] Task 4 — Atualizar página de Aprovação com SwipeCard (AC: 1, 2, 5)
  - [ ] Atualizar `src/app/(dashboard)/aprovacao/page.tsx`
  - [ ] Substituir `<LeadCard />` por `<SwipeCard />` para leads `LOCALIZADOS`
  - [ ] Garantir que o contador de LOCALIZADOS decremente ao aprovar/descartar

## Dev Notes

### Biblioteca de Swipe
[Source: docs/architecture.md#tech-stack]
- Usar `@dnd-kit/core` (já instalado na Story 1.1)
- `useDraggable` hook para capturar posição do arraste
- Threshold de decisão: `Math.abs(deltaX) > 80px`

### Estrutura de Arquivos
[Source: docs/architecture.md#estrutura-do-projeto]
```
src/
├── app/
│   ├── (dashboard)/aprovacao/page.tsx       # Atualizar com SwipeCard
│   └── api/leads/[telefone]/route.ts        # Endpoint PATCH
├── components/approval/
│   └── SwipeCard.tsx                        # Card com lógica de swipe
└── lib/api/
    └── leads.ts                             # updateLeadStatus()
```

### Endpoint PATCH /api/leads/[telefone]
[Source: docs/architecture.md#api-rest]
```typescript
// PATCH /api/leads/5511999998888
// Autenticação: sessão Supabase (Anderson) OU x-api-key (n8n)
// Body: { status: 'PROSPECTAR' }
// Response: { id, empresa, telefone, status }
```

### Fluxo após Aprovação (AC: 3)
[Source: docs/architecture.md#workflow-1]
```
Anderson aprova card
  → PATCH /api/leads/{telefone} { status: 'PROSPECTAR' }
  → n8n lê leads PROSPECTAR e dispara WhatsApp
  → n8n atualiza status para 'PROSPECTADOS'
  → Supabase Realtime notifica o Kanban
```
> **Nota:** O n8n é responsável por disparar o WhatsApp e atualizar para `PROSPECTADOS`. O dashboard apenas muda para `PROSPECTAR` — não dispara WhatsApp diretamente.

### Indicação Visual de Swipe
```typescript
// Exemplo de lógica de cor dinâmica
const dragColor = deltaX > 0 ? 'bg-green-100 border-green-400' : 'bg-red-100 border-red-400'
const dragLabel = deltaX > 0 ? '✓ Aprovar' : '✗ Descartar'
```

### Testing
[Source: docs/architecture.md#testes]
- Testar arrastar card para direita → status muda para PROSPECTAR, card some
- Testar arrastar card para esquerda → status muda para DESCARTADOS, card some
- Testar que cor verde aparece ao arrastar direita e vermelha ao arrastar esquerda
- Testar falha de API → card permanece no painel, mensagem de erro exibida
- Testar em mobile (touch) → swipe com dedo funciona

### Pré-requisito
- Story 2.2 concluída (cards LOCALIZADOS exibidos no painel)

## Change Log
| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-04-15 | 1.0 | Criação inicial | River (SM) |

## Dev Agent Record
_(preenchido pelo @dev durante implementação)_

### Agent Model Used
_

### Completion Notes
_

### File List
_

## QA Results
_(preenchido pelo @qa)_
